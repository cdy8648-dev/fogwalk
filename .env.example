# 이 파일을 복사해서 .env 로 만들고 실제 토큰을 채우세요:  cp .env.example .env
# (.env 는 git 에 커밋되지 않습니다.)

# 퍼블릭 토큰 (pk. 으로 시작) — 런타임 지도 렌더링용
MAPBOX_PUBLIC_TOKEN=

# 시크릿 다운로드 토큰 (sk. 으로 시작) — 빌드 시 Mapbox SDK 다운로드용
MAPBOX_DOWNLOAD_TOKEN=
